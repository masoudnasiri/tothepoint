# ========================================================================
# Procurement Decision Support System - Configuration File
# ========================================================================
# Copy this file to the project root as '.env' and customize as needed

# ========================================================================
# DATABASE CONFIGURATION
# ========================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=procurement_dss
DATABASE_URL=postgresql://postgres:postgres123@db:5432/procurement_dss

# ========================================================================
# BACKEND CONFIGURATION
# ========================================================================
# Secret key for JWT tokens (CHANGE THIS IN PRODUCTION!)
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# JWT token expiration (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ========================================================================
# FRONTEND CONFIGURATION
# ========================================================================
REACT_APP_API_URL=http://localhost:8000

# ========================================================================
# OPTIMIZATION ENGINE CONFIGURATION
# ========================================================================
# Default optimization time limit (seconds)
OPTIMIZATION_TIME_LIMIT=300

# Default solver (CP_SAT, GLOP, CBC)
DEFAULT_SOLVER=CP_SAT

# Enable multi-threading for optimization
OPTIMIZATION_WORKERS=4

# ========================================================================
# SECURITY CONFIGURATION (PRODUCTION)
# ========================================================================
# Change default passwords after first login!
# Session timeout (minutes)
SESSION_TIMEOUT=480

# Maximum login attempts
MAX_LOGIN_ATTEMPTS=5

# ========================================================================
# NOTES:
# ========================================================================
# 1. Change SECRET_KEY before production deployment
# 2. Change all default passwords after first login
# 3. Enable HTTPS for production (use reverse proxy)
# 4. Configure firewall rules
# 5. Set up regular database backups
# ========================================================================

